{
  "name": "datacenter-mgmt",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev.js",
    "dev:backend": "npm run --workspace backend dev",
    "dev:frontend": "npm run --workspace frontend start:dev",
    "build": "npm run --workspace frontend build && npm run --workspace backend build",
    "test": "npm run --workspace frontend test",
    "tsc": "npm run --workspace frontend tsc && npm run --workspace backend tsc"
  },
  "workspaces": [
    "frontend",
    "backend"
  ]
}
